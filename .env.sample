# -------- Speech-to-Text backend --------
# Options: faster-whisper | openai
STT_BACKEND=faster-whisper
FW_MODEL=large-v3
# CPU: int8 (fast), int8_float16; GPU: float16
FW_COMPUTE_TYPE=int8

# -------- YouTube / yt-dlp --------
# Workaround for SABR/NSIG issues; empty to disable
YT_EXTRACTOR_ARGS=youtube:player_client=android

# -------- Upload limits --------
# 500 MB (in bytes)
MAX_UPLOAD_BYTES=524288000

# -------- Translation (DeepL preferred; falls back to Google if not set) --------
# DeepL official API key
DEEPL_API_KEY=

# Google Cloud Translate (v2) via service account JSON:
# Absolute path to your downloaded service account key file.
# e.g., /home/youruser/keys/gcp-translate-sa.json
GOOGLE_APPLICATION_CREDENTIALS=/abs/path/to/service-account.json
